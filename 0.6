sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    server-->>browser: HTTP status code 201 Created  

    Note right of browser: with single page application, browser stays the same page, and it sends no further HTTP-requests
